[package]
name = "rchtmltopdf-pdf"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "rchtmltopdf pdf layer"

[dependencies]
rchtmltopdf-core.workspace = true
# Behind this crate's interface and nowhere else (D12). Already in the lock
# through the conformance harness, which reads PDFs the same way.
lopdf = { version = "0.45", default-features = false }
